import { TrainingSession } from './training-session';

class TrainingSession {
}

describe('TrainingSessions', () => {
  it('should create an instance', () => {
    expect(new TrainingSession()).toBeTruthy();
  });
});
